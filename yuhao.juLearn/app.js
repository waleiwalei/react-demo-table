// import('modules').then(module => {
//     module.log();
// }).catch(error => 'An error occurred while loading the module');
// import { log, unFunc } from 'modules';

// log();
// unFunc();
// document.write('app.js loaded.');


let log = (a) => {
    console.log(a);
}

log(2);